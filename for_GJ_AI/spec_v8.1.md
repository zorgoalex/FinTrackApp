# **ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Веб-приложение для учёта зарплат, доходов и расходов с поддержкой пространств и филиалов, с дизайном Mobile First**

## 1. ОБЩАЯ ИНФОРМАЦИЯ

- **Название проекта:** ФинУчёт
- **Тип:** Веб-приложение (адаптивное, PWA-ready)
- **Язык интерфейса:** Русский
- **Назначение:** Учёт зарплат, расходов и поступлений по проектам в производственной или сервисной компании. Все данные организованы в рамках рабочих пространств (глобальных проектов), каждое из которых может содержать свои филиалы, сотрудников и аналитику.

## 2. АРХИТЕКТУРА

- **Бэкенд:** Supabase (PostgreSQL, Auth, RLS, Storage)
- **Фронтенд:** кастомный SPA (на React/Vue с Tailwind CSS)
- **Аутентификация:** Supabase Auth с поддержкой OAuth (Google, Apple)
- **PWA:** Поддержка манифеста и сервис-воркера.
- **Экспорт:** Excel (через SheetJS), текстовые отчёты.

## 3. РОЛИ ПОЛЬЗОВАТЕЛЕЙ

Вне пространств существует только одна роль — владелец платформы.

Роль владельца: управление пространствами и настройка глобальных параметров системы. Назначается первому зарегистрированному пользователю. В системе может быть только один владелец с максимальными правами управления платформой.

## 4. ПРОСТРАНСТВА (WORKSPACES)

Каждый пользователь может создавать одно или несколько глобальных рабочих пространств, каждое из которых представляет собой изолированное хранилище данных — филиалов, сотрудников, справочников, операций и аналитики.

- Пространство можно удалить только владельцем. При этом оно помечается как удалённое (soft delete), а все связанные с ним данные остаются в базе и недоступны для интерфейса, но могут быть восстановлены администратором.
- Пользователь может быть участником нескольких пространств одновременно, с разными ролями в каждом.
- Пользователи с ролями **member** и **viewer** могут самостоятельно выйти из пространства через интерфейс. При этом отображается подтверждение: "Вы точно хотите покинуть это пространство?". После подтверждения пользователь теряет доступ, а повторное вступление возможно только по приглашению.

### 4.1. Роли в рамках пространства

| Роль   | Возможности                                                             |
| ------ | ----------------------------------------------------------------------- |
| Owner  | Полный контроль, включая управление участниками и удаление пространства |
| Admin  | Управление справочниками, просмотр и редактирование всех данных         |
| Member | Создание и редактирование операций, просмотр аналитики                  |
| Viewer | Только просмотр всех данных без возможности изменений                   |

### 4.2. Приглашение участников

- Владелец пространства может пригласить других пользователей по e-mail.
- Если приглашение не принято в течение определённого срока, владелец может повторно его отправить.
- Приглашения отображаются в личном кабинете приглашённого и направляются по электронной почте.
- Владелец может исключить пользователя из пространства (soft delete). В этом случае пользователь теряет доступ и возможность редактирования, но все его созданные записи (операции, справочники и т.п.) сохраняются в системе и продолжают использоваться для ссылочной целостности и отчётности. В интерфейсе такой пользователь может отображаться как «Удалённый участник».

### 4.3. Переключение между пространствами

- Переключатель активного пространства отображается в шапке приложения.
- Последнее активное пространство автоматически запоминается и восстанавливается при следующем входе пользователя.
- После переключения пользователь видит только данные выбранного пространства.

### 4.4. Технические аспекты

- Все таблицы данных (операции, филиалы, сотрудники, справочники и пр.) имеют поле `workspace_id`.
- Supabase RLS фильтрует доступ к данным по `workspace_id` и роли пользователя.
- Таблица `project_members` содержит участников пространства и их роли:

```sql
project_members (
  id SERIAL PRIMARY KEY,
  workspace_id UUID REFERENCES workspaces(id),
  user_id UUID REFERENCES auth.users(id),
  role TEXT CHECK (role IN ('owner', 'admin', 'member', 'viewer')),
  invited_at TIMESTAMP,
  accepted_at TIMESTAMP
)
```

- Таблица `workspaces` содержит список пространств:

```sql
workspaces (
  id UUID PRIMARY KEY,
  name TEXT,
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMP DEFAULT now(),
  is_deleted BOOLEAN DEFAULT FALSE
)
```

## 5. ФИЛИАЛЫ

- Привязаны к конкретному пространству.
- Переключатель филиалов размещается в шапке приложения.
- Выбранный филиал подставляется во все формы операций.

## 6. ЕДИНАЯ ФОРМА ОПЕРАЦИЙ для Зарплата/Поступления/Расходы

- Тип операции задаётся в одном поле: Зарплата / Поступления / Расходы
- Форма адаптирует состав полей в зависимости от выбранного типа

### Общие поля:

- Дата, Проект, Филиал, Сумма, Примечание

### Дополнительные поля:

- Зарплата: Сотрудник, Задача, Статус ("выдано", "невыдано")
- Поступления: Источник средств, Статус ("поступило", "не поступило")
- Расходы: Услуга, Поставщик, Статус ("оплачено", "в долг", "неоплачено")

### Шаблоны операций:

- На форме добавляется кнопка "Сохранить как шаблон".
- В верхней части формы — кнопка выбора шаблона из выпадающего списка.
- Отдельный экран "Шаблоны операций" позволяет управлять шаблонами и использовать их при создании новых операций.

### Виджет кратких итогов:

- 📊 За сегодня: +50 000 ₽ | За месяц: +1 250 000 ₽
- При тапе — детализация с возможностью копирования или отправки

## 7. ЗАПЛАНИРОВАННЫЕ ОПЕРАЦИИ

Система поддерживает создание **запланированных операций** любого типа: **зарплата, поступления или расходы**. Такие операции позволяют планировать будущие выплаты, поступления или расходы.

### 7.1. Основные параметры

- Тип операции: Зарплата / Поступления / Расходы
- Сумма
- Дата начала (первая дата появления)
- Повторяемость (опционально):
  - ежедневно
  - еженедельно
  - ежемесячно
  - ежегодно
- Привязка к проекту, филиалу, сотруднику и другие поля, как у обычной операции

### 7.2. Поведение

- В день наступления **запланированная операция автоматически создаётся как обычная**, попадает в список операций и может быть отредактирована вручную.
- Сгенерированная операция ничем не отличается от вручную созданной.
- Пользователь может управлять запланированными операциями: создавать, редактировать, удалять, приостанавливать.
- Повторяемость настраивается аналогично задачам в Google Календаре (например, каждое 10-е число месяца).

### 7.3. Интерфейс

- Отдельный раздел "Запланированные операции" в боковом меню
- Просмотр в виде календаря или таблицы
- Возможность приостановки и возобновления расписания
- В аналитике запланированные операции **не учитываются**, пока не будут сгенерированы

## 8. СПРАВОЧНИКИ

- Привязаны к конкретному пространству
- Поддержка: редактирования, фильтрации, добавления

Справочники:

- Сотрудники
- Проекты
- Задачи
- Филиалы
- Услуги
- Поставщики
- Источники средств
- Статусы операций (с полем "Тип операции": зарплата, поступление, расход)

### Архивация:

- Поле `is_archived`
- Архивированные элементы скрываются, но сохраняются для отчётности

### Объединение элементов справочников:

- Только для администратора
- Доступно через интерфейс управления справочником
- Поддерживается выбор двух записей, где одна помечается как основная
- Все связанные операции и данные дублирующей записи переносятся
- Дублирующая запись архивируется с пометкой "объединено"

## 8. АНАЛИТИКА

- Фильтры: период, филиал, проект, сотрудник, услуга, поставщик, тип операции, статус
- Вывод в таблицы с итогами
- Mobile-режим: табы/аккордеоны

## 9. ЭКСПОРТ АНАЛИТИКИ

- Excel с вкладками
- Текстовый отчёт для копирования и отправки

## 10. ДИЗАЙН MOBILE FIRST

- Одноколоночный layout, max-width: 640px
- Главная страница/экран содержит виджеты аналитики за текущий день и текущий месяц: поступления, расходы, зарплата
- На всех формах, справочниках и аналитике внизу слева всегда отображается иконка "Домой" для быстрого перехода на главную страницу
- FAB-кнопка "+" на главной
- Меню, переключатели, аватар — всегда доступны
- Упрощённые формы, крупные кликабельные элементы
- Все действия — без горизонтального скролла
- В шапке доступна строка глобального поиска по операциям (сумма, примечание, контрагент)

## 11. ОБЯЗАТЕЛЬНЫЕ ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

- Хранение автора и редактора каждой записи
- Формат даты: дд.мм.гггг
- Формат чисел: пробелы для тысяч, 2 знака после запятой
- Смарт-дефолты: подстановка последних значений
- Группировка по дате в списках
- Свайпы: редактирование, удаление, дублирование

