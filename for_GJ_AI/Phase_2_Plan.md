# Фаза 2: Управление рабочими пространствами и ролями

## 🎯 ЦЕЛЬ ФАЗЫ
Реализовать полноценную систему управления workspace с ролями, приглашениями участников и мультипользовательской работой.

## 📋 ДЕТАЛЬНЫЙ ПЛАН ЗАДАЧ

### 2.1 Расширение схемы БД для мультипользователя (приоритет: HIGH)

#### 2.1.1 Создание таблицы приглашений
```sql
-- Таблица для управления приглашениями
CREATE TABLE workspace_invitations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  workspace_id UUID REFERENCES workspaces(id) ON DELETE CASCADE,
  invited_by UUID REFERENCES auth.users(id),
  invited_email TEXT NOT NULL,
  role TEXT CHECK (role IN ('admin', 'member', 'viewer')) NOT NULL,
  status TEXT CHECK (status IN ('pending', 'accepted', 'declined', 'expired')) DEFAULT 'pending',
  invitation_token UUID DEFAULT gen_random_uuid(),
  invited_at TIMESTAMP DEFAULT now(),
  expires_at TIMESTAMP DEFAULT (now() + interval '7 days'),
  accepted_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now()
);
```

#### 2.1.2 Обновление RLS политик для мультипользователя
- Расширить политики workspace_members для поддержки всех ролей
- Добавить политики для workspace_invitations
- Протестировать доступ для разных ролей

#### 2.1.3 Создание индексов для производительности
```sql
CREATE INDEX idx_workspace_invitations_email ON workspace_invitations(invited_email);
CREATE INDEX idx_workspace_invitations_token ON workspace_invitations(invitation_token);
CREATE INDEX idx_workspace_members_workspace_user ON workspace_members(workspace_id, user_id);
```

### 2.2 UI для управления workspace (приоритет: HIGH)

#### 2.2.1 Страница управления workspace
- **Путь**: `/workspace/:workspaceId/settings`  
- **Компонент**: `WorkspaceSettingsPage.jsx`
- **Разделы**:
  - Основная информация (название, описание)
  - Участники и их роли
  - Приглашения (активные/истекшие)
  - Опасная зона (удаление workspace)

#### 2.2.2 Модальные окна для управления
- **InviteUserModal.jsx** - приглашение нового участника
- **ChangeRoleModal.jsx** - изменение роли участника  
- **RemoveUserModal.jsx** - исключение участника
- **DeleteWorkspaceModal.jsx** - удаление workspace

#### 2.2.3 Компонент переключателя workspace
- **WorkspaceSwitcher.jsx** - выпадающий список в шапке
- Показывает доступные workspace пользователя
- Отображает текущую роль в каждом workspace
- Переключение с сохранением в localStorage

### 2.3 Система приглашений (приоритет: MEDIUM)

#### 2.3.1 Backend функции (Supabase Edge Functions)
```javascript
// invite-user.js
export async function inviteUser(workspaceId, email, role, invitedBy) {
  // Создание приглашения
  // Отправка email с токеном
  // Возврат статуса
}

// accept-invitation.js  
export async function acceptInvitation(token) {
  // Проверка валидности токена
  // Создание записи в workspace_members
  // Обновление статуса приглашения
}
```

#### 2.3.2 Email шаблоны
- **HTML шаблон** приглашения с брендингом
- **Текстовая версия** для совместимости
- **Ссылка на принятие** приглашения

#### 2.3.3 Страницы приглашений
- **InvitationPage.jsx** - страница принятия приглашения
- **MyInvitationsPage.jsx** - список приглашений пользователя

### 2.4 Расширение WorkspaceContext (приоритет: HIGH)

#### 2.4.1 Обновление контекста
```javascript
const WorkspaceContext = {
  // Существующие
  currentWorkspace,
  loading,
  error,
  workspaceId,
  
  // Новые
  userRole,
  allWorkspaces,        // Все доступные workspace
  switchWorkspace,      // Функция переключения
  canInviteUsers,       // Проверка прав
  canManageRoles,       // Проверка прав
  canDeleteWorkspace,   // Проверка прав
  
  // Функции управления
  inviteUser,
  removeUser,
  changeUserRole,
  deleteWorkspace
}
```

#### 2.4.2 Хуки для проверки прав
```javascript
// usePermissions.js
export function usePermissions() {
  const { userRole } = useWorkspace();
  
  return {
    canInvite: ['owner', 'admin'].includes(userRole),
    canManageRoles: userRole === 'owner',
    canDelete: userRole === 'owner',
    canEditOperations: ['owner', 'admin', 'member'].includes(userRole)
  };
}
```

### 2.5 Обновление навигации и Layout (приоритет: MEDIUM)

#### 2.5.1 Улучшение боковой панели
- Добавить пункт "Настройки workspace" (только для owner/admin)
- Показывать текущую роль пользователя
- Индикатор количества участников

#### 2.5.2 Переключатель workspace в шапке
- Красивый выпадающий список
- Поиск по названию workspace
- Индикация ролей
- Кнопка "Создать новое пространство"

#### 2.5.3 Breadcrumbs навигация
- Показывать путь: Workspace > Раздел > Страница
- Кликабельные элементы для быстрой навигации

### 2.6 Создание новых workspace (приоритет: LOW)

#### 2.6.1 Страница создания
- **CreateWorkspacePage.jsx**
- Форма с названием и описанием
- Выбор шаблона (личное/командное)
- Автоматическое создание роли Owner

#### 2.6.2 Мастер настройки
- Пошаговый мастер после создания
- Приглашение первых участников
- Настройка базовых справочников (опционально)

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### База данных
- Расширить workspace_members для поддержки всех ролей
- Добавить workspace_invitations для приглашений
- Обновить RLS политики под новые роли
- Добавить поля last_accessed_at в workspace_members

### Безопасность
- Проверка прав на backend через RLS
- Валидация email адресов при приглашении
- Ограничение на количество приглашений (защита от спама)
- Токены приглашений с ограниченным временем жизни

### UX/UI
- Четкое отображение ролей и прав
- Подтверждения для критических действий
- Загрузочные состояния для всех операций
- Мгновенная обратная связь при изменениях

## 📅 ВРЕМЕННЫЕ РАМКИ

### Неделя 1: База данных и backend
- Создание таблиц и индексов
- Обновление RLS политик  
- Создание Edge Functions для приглашений

### Неделя 2: Core UI компоненты
- WorkspaceContext расширение
- Основные модальные окна
- Хуки для проверки прав

### Неделя 3: Страницы управления
- WorkspaceSettingsPage
- Система приглашений (фронтенд)
- Переключатель workspace

### Неделя 4: Тестирование и полировка
- Тестирование всех ролей
- Email шаблоны
- Исправление багов и UX улучшения

## 🎯 КРИТЕРИИ ГОТОВНОСТИ

### Must Have (обязательно):
- ✅ Owner может приглашать/исключать участников
- ✅ Система ролей работает корректно (Owner/Admin/Member/Viewer)
- ✅ Переключение между workspace
- ✅ RLS корректно фильтрует данные по ролям

### Should Have (желательно):
- ✅ Email уведомления о приглашениях
- ✅ Красивый UI для управления участниками
- ✅ Breadcrumbs навигация

### Could Have (опционально):
- ⏳ Мастер создания workspace
- ⏳ Шаблоны workspace
- ⏳ Аудит лог действий участников

## 🔗 СВЯЗЬ С ДРУГИМИ ФАЗАМИ

**Подготавливает для Фазы 3:**
- Многопользовательские операции с авторством
- Права доступа к редактированию операций
- Совместную работу над справочниками

**Использует из Фазы 1:**
- Существующий WorkspaceContext
- Систему аутентификации
- Базовые workspace и workspace_members таблицы